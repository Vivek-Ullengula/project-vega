# Project Vega Frontend

React/Vite chat UI for the Coaction underwriting assistant.

## Commands

```bash
npm ci
npm run dev
npm run lint
npm run typecheck
npm run build
```

The production Docker build compiles this app and serves `frontend/dist` from the FastAPI backend.
